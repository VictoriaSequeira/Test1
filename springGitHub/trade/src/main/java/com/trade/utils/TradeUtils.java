package com.trade.utils;

import java.util.ArrayList;
import java.util.Collections;
import java.util.Comparator;
import java.util.Map;
import java.util.Map.Entry;
import java.util.stream.Collectors;

import com.trade.beans.Trade;

public class TradeUtils {

	/*
	 * public void getMaxVersion(Map<String, Trade> tradeRepo) { ArrayList<String>
	 * collectArray = tradeRepo.keySet() .stream()
	 * .collect(Collectors.toCollection(ArrayList::new)); Trade trade =
	 * collectArray.stream() .max(Comparator.comparingInt(collectArray::getVersion))
	 * .get();
	 * 
	 * }
	 */
}
